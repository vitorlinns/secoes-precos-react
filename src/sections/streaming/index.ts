import type { SectionMeta } from '../../types/section'
import { StreamingSection } from './StreamingSection'

export const meta: SectionMeta = {
  id: 'streaming',
  title: 'Streaming',
  description: 'Tabela comparativa dark, estilo serviço de streaming, com coluna em destaque.',
  component: StreamingSection,
}
